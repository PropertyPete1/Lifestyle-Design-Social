export async function createTicket(title: string, description: string) {
  // Replace with actual API call later
  console.log('[Mock Ticket Created]', { title, description });
  return { success: true };
} 