import axios from "axios";

export async function generateCartoonVideo(prompt: string): Promise<string> {
  // Simulated output until actual API is integrated.
  return `https://example.com/cartoon-videos/${encodeURIComponent(prompt)}.mp4`;
} 