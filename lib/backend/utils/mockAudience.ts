export async function getMockedAudienceData(platform: string) {
  // Placeholder: Replace with real analytics once available
  return {
    platform,
    peakHours: [9, 12, 15, 18],
    engagementRate: Math.random(),
  }
} 