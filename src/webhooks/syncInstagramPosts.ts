// Placeholder: To be wired with Instagram Graph API if needed
export async function syncInstagramCaptions(): Promise<void> {
  // Fetch recent IG posts and store captions + views/likes
  // storeCaption(caption, engagementScore, hashtags)
} 